## 修改内容

1. **PlayerView.vue** - `controls-section` 的 `align-items` 从 `center` 改为 `stretch`
   - 让子元素（PlayerControls 和 PlayerVolume）填满整个宽度

2. **PlayerControls.vue** - `justify-content` 从 `flex-start` 改为 `space-between`
   - 让按钮在整行内均匀分布，两端对齐

这样布局会更美观，按钮会均匀分布在第一行，音量控制会填满第二行。