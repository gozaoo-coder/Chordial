## 任务目标
统一整个项目的代码注释语言，仅允许使用英文和简体中文。

## 项目现状分析
- **项目类型**：Tauri + Vue3 音乐播放器应用
- **当前注释状态**：所有注释均为简体中文，未发现其他语言
- **文件数量**：
  - Rust 文件：27 个
  - 前端文件：63 个 (JS/TS/Vue)

## 执行计划

### 阶段一：全面扫描检查
1. 扫描所有 Rust 文件中的注释
2. 扫描所有前端文件中的注释
3. 识别所有非英文/简体中文的注释内容
4. 生成需要修改的注释清单

### 阶段二：Rust 后端注释处理
按模块逐个处理：
1. `audio_metadata/` 模块 - 音频元数据读取
2. `cache/` 模块 - 缓存管理
3. `music_source/` 模块 - 音乐源管理
4. `scanner/` 模块 - 扫描器
5. `lyric_parser/` 模块 - 歌词解析
6. 根目录文件：`lib.rs`, `error.rs`, `main.rs` 等

### 阶段三：前端注释处理
按目录逐个处理：
1. `src/api/` - API 接口
2. `src/class/` - 类定义
3. `src/components/` - Vue 组件
4. `src/stores/` - 状态管理
5. `src/views/` - 页面视图
6. `src/utils/` - 工具函数

### 阶段四：验证与检查
1. 重新扫描所有文件，确认无遗漏
2. 运行项目构建，确保无语法错误
3. 运行 lint 检查

## 注释规范标准

### 允许的注释语言
- ✅ 简体中文（如：// 读取文件内容）
- ✅ 英文（如：// Read file content）
- ❌ 日文、韩文、繁体中文等其他语言

### 注释格式
- **Rust**：支持 `//!`, `///`, `//`, `/* */` 等标准 Rust 注释
- **前端**：支持 `//`, `/* */`, `<!-- -->` (Vue 模板), `/** */` (JSDoc)

### 处理策略
由于当前注释已经是简体中文，主要工作：
1. 检查是否有混入的其他语言字符
2. 确保注释清晰、准确
3. 统一注释风格

## 预期产出
1. 所有代码注释仅包含英文或简体中文
2. 保持原有注释含义不变
3. 项目可正常编译运行

请确认此计划后，我将开始执行具体的注释统一工作。